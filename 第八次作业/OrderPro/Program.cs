using System;

namespace OrderPro
{
    class Program
    {
        static void Main(string[] args)
        {
          /*  Guest guest1 = new Guest(1001, "Sean");
            Guest guest2 = new Guest(1002, "Bark");

            Product apple = new Product(101, 2.00, "苹果");
            Product banana = new Product(102, 1.00, "香蕉");
            Product orange = new Product(103, 3.50, "橘子");

            Order order1 = new Order(guest1, 001);
            order1.AddDetail(apple, 2);
            order1.AddDetail(orange, 1);
            OrderService.AddOrder(order1);

            

            Order order2 = new Order(guest2, 002);
            order2.AddDetail(apple, 1);
            order2.AddDetail(banana, 2);
            order2.AddDetail(orange, 2);
            OrderService.AddOrder(order2);
            
            Order order3 = new Order(guest1, 003);
            order3.AddDetail(apple, 3);
            order3.AddDetail(orange, 2);
            OrderService.AddOrder(order3);

            OrderService.Export("D:\\demo.xml");
            OrderService.ClearOrder();
            OrderService.Import("D:\\demo.xml");

            Console.WriteLine("select by id:");
            Console.WriteLine(OrderService.selectOrderByID(001));
                
            Console.WriteLine("select by guest:");
            foreach (Order order in OrderService.selectOrderByGuest(1001, "Sean"))
            {
                Console.WriteLine(order);
                Console.WriteLine();
            }
            Console.WriteLine("normal sort");
            OrderService.sortOrder();
            OrderService.ShowAllOrder();

            Console.WriteLine("sort by payment");
            OrderService.sortOrder((orderA,orderB) => (int)(orderA.TotalPayment - orderB.TotalPayment));
            OrderService.ShowAllOrder();
            Console.ReadLine();
*/
        }
    }
}
